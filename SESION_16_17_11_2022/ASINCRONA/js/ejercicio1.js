/*En base al siguiente código, añadir una función para restar dos números: */
let restar = (x,y) => {return x-y;};
let one = parseFloat(prompt('Ingrese el primer numero: '));
let two = parseFloat(prompt('Ingrese el segundo numero: '));
var resultado = restar(one,two);
alert(resultado);